/**
 * @fileoverview Defines Diff component
 */
import * as React from "react";

interface Props { }

/**
 * Diff renders a diff between two line states
 * @param {Props} props Props passed to the component
 */
const Diff = (props: Props): React.ReactElement => {
  return (
    <clipPath></clipPath>
  );
};

export {
  Diff,
};
