/**
 * @fileoverview Defines Grid component
 */
import * as React from "react";

interface Props { }

/**
 * Grid renders a grid
 * @param {Props} props Props passed to the component
 */
const Grid = (props: Props): React.ReactElement => {
  return (
    <>
    </>
  );
};

export {
  Grid,
};
